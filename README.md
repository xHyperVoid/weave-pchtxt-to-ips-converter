# weave-pchtxt-to-ips-converter
Weave is a simple CLI tool designed to convert .pchtxt patch files into the .ips (IPS32) binary format required by the Nintendo Switch modding environment (Atmosphère).
